// The 'architecture' project factory now lives in todl (the headless engine that
// owns the project ecosystem). Re-exported here so the module registration and
// consumers keep their import path.
export { ArchitectureProjectFactory } from '@pragmatic-tech-ai/todl'
