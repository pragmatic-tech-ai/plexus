// The TODL-authoring project base + its scaffold surface now live in todl (the
// headless engine). Re-exported here so plexus consumers keep their import path.
export {
    TodlProjectFactory,
    isTodlProject,
    CLAUDE_MD_FILENAME,
    CLAUDE_DIR,
    TODL_BASE_SCAFFOLD,
    type ScaffoldFile,
} from '@pragmatic-tech-ai/todl'
